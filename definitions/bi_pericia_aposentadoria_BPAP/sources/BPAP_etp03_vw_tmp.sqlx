config {
    type: "view",
    tags: ["BPAP"],
    name: "BPAP_etp03_vw_tmp",
    description: ""
}

SELECT distinct
   A.*, --except(peritos_por_junta)
   B.peritos_por_junta
FROM  ${ref("BPAP_etp01_vw_tmp")} A
  LEFT JOIN ${ref("BPAP_etp02_vw_tmp")} B ON A.ID_JUNTA = B.ID_JUNTA